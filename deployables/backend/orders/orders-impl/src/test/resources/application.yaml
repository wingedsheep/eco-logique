spring:
  main:
    allow-bean-definition-overriding: true
  mvc:
    problemdetails:
      enabled: true
  flyway:
    enabled: true
    locations: classpath:db/migration/orders
    schemas: orders
    default-schema: orders
    create-schemas: true