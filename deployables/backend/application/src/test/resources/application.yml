spring:
  profiles:
    active: test
  mvc:
    problemdetails:
      enabled: true
  flyway:
    enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/test