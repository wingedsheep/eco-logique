spring:
  main:
    allow-bean-definition-overriding: true
  mvc:
    problemdetails:
      enabled: true
  flyway:
    enabled: true
    locations: classpath:db/migration/cart
    schemas: cart
    default-schema: cart
    create-schemas: true
